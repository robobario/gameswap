sampleProperty: hello!

redirectAllToHttps: true
googleSecret: ${GOOGLE_SECRET:-fakefakefake}

# Database settings.
database:
  driverClass: org.h2.Driver
  user: gameswap
  password: gameswap
  url: jdbc:h2:mem:https

server:
  rootPath: '/gameswap/*'
  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0
  requestLog:
    appenders:
      - type: console